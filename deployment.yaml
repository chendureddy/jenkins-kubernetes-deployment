apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  labels:
    app: nodeapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodeapp
  template:
    metadata:
      labels:
        app: nodeapp 
    spec:
      containers:
      - name: nodeapp
        image: chendureddy/nodeapp:latest
        ports:
        - containerPort: 3000
